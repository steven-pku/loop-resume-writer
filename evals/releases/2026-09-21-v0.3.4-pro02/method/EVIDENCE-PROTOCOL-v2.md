# 证据协议 v2 — 逐轮可见文本与页面衔接

日期：2026-09-21；适用本批固定六例，不改变行为断言、运行候选、任务消息或尝试次数。

## 更正

原交接包01-operator/RUN.md:30、:36–37以及02-grader-only/GRADING.md:55把缺少原生线程ID时的连续录屏设为必需。该要求对当前已授权浏览器工具过严，现以本协议替代；旧包原件不回写。

连续录屏、截图和官方内容导出均非不可替代条件。它们未获得就如实注明，不要求购买、安装、另建服务或读取后台。UI标签本身不认证后端，录屏也不独立认证后端。

## 本批最小可接受证据

每例一份OPERATOR-RECORD.md；每个已执行回合各一份实际输入和完整可见输出。

1. 开始前：从当前界面照录临时、不个性化、模型和能力标签；记录唯一附件文件名；照录浏览器工具实际返回的标签页标识。不上传本报告、判卷资料、旧输出或其他case材料。
2. 当前轮：发送对应sessions文件全文。保存实际发送输入，不用未发送的计划文件冒充页面回显；不预发后续轮次。
3. 完成后：在原标签保存完整可见回答（允许扁平化但不删文字），照录页面实际显示的完成信号和时间、工具可见范围。无需隐藏思考文本、后台请求或凭证。
4. 有下一轮时：下一次发送前，在同一工具标签内保存前一轮用户消息和助手回答各一段可识别的页面文字，随后发送预定下一条。此证据与先前完整I/O由GPT Pro核对，主控不分析答案。只能用真实已完成页面，不能让模型编写连续对话。
5. 最后一轮：保存输出和完成信号后停止；不重新生成、不编辑已发消息、不分叉、不换模型。没有下一条预定输入时，模型追问也不临时追加。

记录中的“浏览器标签标识”是工具层句柄，不是原生模型线程ID。同一标签标识本身不足以证明续接；与前轮可见历史、逐轮输入输出和发送/完成顺序合用，只支持本批可见UI层面的真实续接，不证明后台会话ID或绝对信息隔离。

## 对缺失项的处理

- 工具逐项日志没有捕获：NOT_CAPTURED；界面确实不暴露：NOT_EXPOSED。不要把模型的“我用了python”抄成操作者工具日志。
- 原生线程ID未暴露：NOT_EXPOSED；截图没保存：NOT_CAPTURED；录屏接口不支持：UNSUPPORTED；浏览器UI无进程退出码：NOT_APPLICABLE_UI。未执行轮次继续NOT_RUN。
- 事后计算SHA-256只固定这次收到的文件，不认证此前执行来源，不拿它代替页面证据。
- 不需要补R01截图或重跑R01；R01作为操作者记录＋返回正文的有限证据通过单独保留。

## 停止边界（只停止受影响项，不回写已完成轮次）

- 不能继续确认当前页保有上一轮记录，或已换页/分叉/重生成却无法保留经过：停止该多轮case，回传已有材料。GPT Pro记连续性证据不足；不在新会话粘历史凑足轮次。
- 完整输出无法保存、输出中断或服务报错：保存现有正文和可见错误，不重试；未执行后续轮次NOT_RUN。
- 无法维持已授权的GPT Pro/不个性化执行条件、上传了判卷材料或候选加载失败：保留真实记录，停止受影响case，不自行更换路径。

这些是有限范围证据的接受规则，不构成完整宿主、网络动作、读取动作或后端身份的独立认证。内容明确失败仍按原断言FAIL/PARTIAL；记录缺失与内容缺陷分字段，不把两者混成一个自动结论。
