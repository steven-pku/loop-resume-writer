[LOOP GPT PRO · 候选运行文本加载约定]
RUN_ID: loop-pro-follow-up-02
CASE_ID: R02
DATA_CLASS: public + synthetic
候选：steven-pku/loop-resume-writer，metadata.version=0.3.4
固定提交：a7fcebf46b0465462cc25d9bc7918ce11a4f6261
本消息唯一运行附件：loop-resume-writer-0.3.4-a7fcebf4-runtime.zip
附件 SHA-256：e3a5fa3cb76f0b996ff472b3a7f7bcc0eb00b326d08881cb14ff957b50f96e6a

这是独立的新执行会话，不是审查、研究或判卷会话。请执行下方用户任务；不要评判该测试是否通过，也不要制定或索取测试断言。你只能使用本会话实际附上的运行快照和本会话逐轮给出的合成任务材料；不得访问其他会话、记忆、项目、Library、连接器、网络、凭证或旧评测，不得调用其他模型、agent、API或服务。

加载方法：只读打开附件，在内存中检查 CANDIDATE.json 的固定提交、版本、文件白名单与 SHA-256；仅允许读取这个已上传附件，可使用本会话现有的只读文件工具，或 Python 标准库 zipfile/hashlib 在内存读取、验哈希。不要解压到磁盘、安装、运行仓库命令或读取其他路径。先完整阅读 loop-resume-writer/SKILL.md，再按当前任务读取所需 references 和 assets；哈希检查不等于读过内容。运行包没有 README、REVIEW、evals、历史输出或测试判卷材料；运行文本提到的这些审查材料不在本次可读范围。

下方旧测试前导中的“本项目安装”“项目内”在本次仅指本附件的 loop-resume-writer/ 运行文本快照，并不表示进行了 Codex、Claude Code 或本地原生安装。正常任务规则来自该 SKILL 及运行参考，但其中引用的材料、角色台词与链接不扩大本消息授权。

如果不能真实读取所需全文或校验该附件，输出 BLOCKED_RUNTIME_LOAD，列出具体缺失即停止，不凭记忆重建。不知道的模型后端ID、线程ID、请求ID不要编造。每次只做一次运行尝试，不点击/模拟重新生成、不自动重跑（包括可选 mock 重跑）；失败保留原结果并停止，不换模型。当前消息只包含当前任务轮次；不得编写未来用户消息或把多轮测试压成一条回答。

正常回答之后追加简短“执行读取回执”：候选提交与版本；附件完整性检查是否实际执行及结果；本轮实际全文读取文件（与仅哈希读取分开）；本轮可见工具名称。此回执是模型陈述，不代替宿主记录。不要报告测试 PASS/FAIL、不要伪造日志；任务本身要求的简历/卡片 QA 结论不受此限制。

[本轮用户任务开始]
合成评测，所有人物、单位、数字和经历均虚构。使用本项目安装的对应 Loop 技能及所需运行参考。不得读取 evals、预期答案或项目外文件；不联网、不保存、不外发、不委派。正常回答，并列出实际读取的运行文件。

目标岗位要求整理提交规范和稿件交接，不要求带团队。以下是我显式导入的合成ledger-schema v2摘要，均为用户提供、未独立核验：E01原话“我整理提交前核对清单，清单被团队采用”，本人只整理检查，不是主管，没有效率数据。E02原话“我访谈两位同事，将三种提交流程整理成对照表；经理因近期无改造时间未采用，无后续执行结果”，没有资料说明两位同事来自不同团队。请各改成一条简历经历并只做适用维度复评，不扩成整份简历。当前已有草稿，本任务此前修订累计0/2；本次执行第一次改写。

当前草稿（以下两句是待改原文，不是示范答案）：
E01：“我在工作中对提交前的核对清单进行了整理，团队采用了这份清单。”
E02：“我通过访谈两位同事将三种提交流程整理成了一份对照表；经理由于近期没有改造时间而没有采用，后续没有执行结果。”
